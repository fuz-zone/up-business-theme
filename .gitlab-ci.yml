image: registry.gitlab.com/pages/hugo/hugo_extended:latest

variables:
  GIT_SUBMODULE_STRATEGY: recursive

pages:
  script:
  - cp -rf hugoBasicExample/* ./
  - hugo
  artifacts:
    paths:
    - public
  only:
  - master
